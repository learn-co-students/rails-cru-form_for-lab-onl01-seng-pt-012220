<h1>Update <%= @artist.name %> Info </h1>

<%= form_for @artist do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :bio %>
    <%= f.text_area :bio %>

    <%= f.submit %>
<% end %>

<%= link_to "Return to Artists List", artists_path %>